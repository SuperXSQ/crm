<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.xsq.crm.workbench.dao.ActivityRemarkDao">

    <update id="editActivityRemark">

        update tbl_activity_remark set
            noteContent = #{noteContent},
            editTime = #{editTime},
            editBy = #{editBy},
            editFlag = #{editFlag}
        where
            id = #{id}

    </update>


	<select id="getRemarkById" resultType="ActivityRemark">

		select * from tbl_activity_remark where id = #{id}

	</select>


	<insert id="addRemark">

		insert into tbl_activity_remark (
			id,
			noteContent,
			createTime,
			createBy,
			editFlag,
			activityId
		)
		values (
			#{id},
			#{noteContent},
			#{createTime},
			#{createBy},
			#{editFlag},
			#{activityId}
		)
	</insert>


	<delete id="deleteById">

		delete from tbl_activity_remark where id = #{id}

	</delete>


	<select id="getRemarkListByAid" resultType="ActivityRemark">

		select * from tbl_activity_remark where activityId = #{activityId} order by createTime asc

	</select>


	<select id="getCountByAid" resultType="int">

		select count(*) from tbl_activity_remark where activityId in

		<foreach collection="array" item="aid" open="(" separator="," close=")">
			#{aid}
		</foreach>

	</select>


	<delete id="deleteByAid" >

		delete from tbl_activity_remark where activityId in

		<foreach collection="array" item="aid" open="(" separator="," close=")">
			#{aid}
		</foreach>

    </delete>

</mapper>
























